Router 耗时？
Tavily 单次调用耗时？
Exa 单次调用耗时？
并行后总搜索耗时？
总结阶段输入 token 数 + 耗时？

看调用参数：Tavily 是否用了 search_depth="advanced" 或 research？
Exa 是否用了 mode="deep"？

立刻改搜索模式：Tavily 改成 fast 或 ultra-fast，Exa 改成 instant 或 fast。
确保真正并行：用 asyncio.gather() 同时调用 Tavily 和 Exa。
引入 Map-Reduce：先并行对每个搜索结果做轻量局部总结（用 DeepSeek V3 或 Claude Haiku），再让 Claude 4.6 做最终合并。
加缓存：Semantic Cache（Redis）对重复查询非常有效。
Router 智能拆查询：让 DeepSeek 把一个大问题拆成 3~5 个小查询并行搜索。

